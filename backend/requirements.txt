# ============================================================
# JurisRoute MA — Dépendances Python (Python 3.11)
# ============================================================
# Installation rapide :
#   pip install -r requirements.txt
#
# Backend Groq (recommandé) ou Ollama → ce fichier suffit
# Backend Qwen2 (GPU) → ajouter requirements-qwen2.txt
# ============================================================

# ── API & Serveur ────────────────────────────────────────────
fastapi==0.112.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.8.2
httpx>=0.27.0,<0.28.0
python-dotenv==1.0.1

# ── RAG — Embeddings & Recherche vectorielle ─────────────────
sentence-transformers==3.0.1
faiss-cpu==1.14.2         # remplacer par faiss-gpu si GPU disponible
rank-bm25>=0.2.2           # recherche hybride BM25 + FAISS
numpy>=1.26.0,<2.0.0

# ── HuggingFace Hub ──────────────────────────────────────────
# Téléchargement automatique du modèle d'embedding
huggingface_hub>=0.24.0,<1.0.0
tokenizers>=0.19.0
sentencepiece>=0.2.0       # tokenizer multilingue (MiniLM multilingue)

# ── OCR & Documents ──────────────────────────────────────────
pymupdf==1.24.9
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==10.4.0
opencv-python-headless>=4.9.0,<4.11.0  # prétraitement images

# ── Indexation dataset ───────────────────────────────────────
datasets==2.21.0
